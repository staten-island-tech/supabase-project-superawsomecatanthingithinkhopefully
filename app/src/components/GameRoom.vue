<template>
  <div>
    <p>YOU DID IT!</p>
  </div>
</template>

<script setup lang="ts">
import { supabase } from '@/lib/supabaseClient'

async function f() {
  //get userid before
  //put it in penia later
  //then data. to get player number
  //const { data, error } = await supabase.from('game').select().eq('id', userid)
}
</script>

<style scoped></style>
